import * as React from "react";
import * as ReactDOM from "react-dom/client";
import GuidesPage from "../page-components/Guides.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GuidesPage />
  </React.StrictMode>,
);
