import * as React from "react";
import * as ReactDOM from "react-dom/client";
import AboutMePage from "../page-components/About.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AboutMePage />
  </React.StrictMode>,
);
